def copy_string_recursive(src, index=0):
if index == len(src):
return ""
return src[index] + copy_string_recursive(src, index + 1)
source_string = input("Enter the source string to be copied: ")
copied_string = copy_string_recursive(source_string)
print(f"The copied string is: {copied_string}")
